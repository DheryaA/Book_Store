import Rings from 'react-simple-dots-loader';

const Loader = () => {
    return (
        <div>
          <Rings color='red' height={80} width={80} />
        </div>
      );
}

export default Loader
